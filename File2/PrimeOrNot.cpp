/*#include<stdio.h>
int main() {
	int no = 0, i = 0, flag = 0;
	printf("Enter any no");
	scanf_s("%d", &no);
	if (no == 1) {
		printf("No is neither a prime nor a composite no");
	}
	else {

		for (int i = 2; i < no; i++) {
			if (no % i == 0) {
				flag++;
				break;
			}
		}
		if (flag == 1)
			printf("Given number is not prime");
		else
			printf("Given number is prime");
	}
	return 0;
}*/
	

